import React from 'react'

const ListComponents = (props) => {
    
    return (
            
        <div>
            
            <h1>{props.tex}</h1>
            
        </div>
    );
    
};

export {ListComponents};
